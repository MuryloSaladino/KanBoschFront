import { Typography } from "@mui/material";

export default function Logo() {
    return(
        <Typography
            variant="h2"
            color="primary.main"
            textAlign="center"
        >
            KanBom
        </Typography>
    )
}