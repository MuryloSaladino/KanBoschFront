import { createTheme } from "@mui/material";

const appTheme = createTheme({
    palette: {
        primary: {
            main: "#00B4D8",
            light: "#90E0EF",
            dark: "#03045E"
        },
        background: {
            paper: "#242337"
        }
    }
})

export default appTheme